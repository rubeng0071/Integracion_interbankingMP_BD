# Requirements del proyecto monolítico (compatibilidad).
# En el Bloque 2 cada componente (mp_webhook_function/, ib_poller/) tendrá su propio requirements pinneado.
#
# Instalación: pip install -r requirements.txt

# === Core: API + datos ===
requests==2.32.3
pandas==2.2.3
python-dateutil==2.9.0.post0
openpyxl==3.1.5

# === Base de datos ===
pyodbc==5.1.0

# === Azure (Key Vault, Managed Identity) ===
# Opcionales en local; obligatorios cuando se despliega.
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0

# === Logging estructurado (CAL-04) ===
python-json-logger==2.0.7

# === Testing (CAL-12) ===
pytest==8.3.3
pytest-cov==5.0.0
